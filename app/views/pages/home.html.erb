<h1>Time elapsed</h1>
<div class="second-container">
  <h3 id="second-count">0</h3>
  <h3 id="second-word">seconds</h3>
</div>

<div class="grid-container">
  <% @randomised_images.each do |image| %>
    <div class="card-holder">
      <img class="hidden" src="<%= image %>" alt="">
    </div>
  <% end %>
</div>
